# App

`app` contains the application implementation skeleton.

- `backend`: FastAPI service for loading cases and producing diagnosis output.
- `frontend`: React/Vite workbench for engineers.

The first runnable milestone should support offline demo mode with no model Token.
