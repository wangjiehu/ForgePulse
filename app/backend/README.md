# ForgePulse Backend

Backend responsibilities:

- Load sample cases.
- Validate data contracts.
- Analyze sensor anomalies.
- Parse alarms.
- Retrieve SOP and maintenance evidence.
- Compose deterministic diagnosis.
- Later: call an LLM provider through a provider abstraction.

Do not hard-code API tokens. Use environment variables when model access is added.
